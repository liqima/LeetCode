class Solution:
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        # the first solution
        return len(nums) != len(set(nums))
        
