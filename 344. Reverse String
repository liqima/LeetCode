class Solution:
    def reverseString(self, s):
        """
        :type s: str
        :rtype: str
        """
        res = ''
        for i in range(len(s)):
            res += s[len(s) - i - 1]
        return res 

s = Solution()
print(s.reverseString('hello'))
